#include <stdio.h>

/**
 * main - starting point
 *
 * Return: 0 (Success)
 */
int main(void)
{
	/* Print the specified message */
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}

