#include "main.h"

int main(void)
{
	char *str = "\"At the end of the day, my goal was to be the best hacker\"\n\t- Kevin Mitnick";
	int i = 0;
        while (str[i] != '\0') {
		        _putchar(str[i]);
			        i++;
				    }
	    return (0);
}
