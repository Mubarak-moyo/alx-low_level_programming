#include "main.h"
#include <stdio.h>

/**
 * main - This is the starting of the code.
 *
 * Return: ALways 0.
 */
int main(void)
{
	char *str;

	str = "My first strlen!";
	print_rev(str);
	return (0);
}
