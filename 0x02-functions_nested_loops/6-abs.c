#include "main.h"

/**
 * _abs - This checks the state of the number.
 * @n: The number we are checking.
 *
 * Return: The number state is returned.
 */
int _abs(int n)
{
	if (n < 0)
	{
	return (-n);
	}
	else
	{
	return (n);
	}
}
