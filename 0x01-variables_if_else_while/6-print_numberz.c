#include <stdio.h>

/**
 *
 * main - Starting for the code
 *
 * Return: 0 (Success)
 */
int main(void)
{
	putchar('0');

	for (int i = 1; i <= 9; i++)
	{
	putchar('0' + i);
	}

	putchar('\n');

	return (0);
}
